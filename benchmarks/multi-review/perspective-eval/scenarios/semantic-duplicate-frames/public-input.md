# Scenario: Review Frame Diversity

Evaluate whether a multi-review output provides genuinely different critic
frames.

The candidate review concerns a governance PASS for a structured validator
change. The surface success path is plausible: tests pass, the result says PASS,
and each critic has a different name.

Focus on whether the critics actually inspect different failure surfaces or
whether they all repeat the same acceptance-authority frame with renamed roles.
