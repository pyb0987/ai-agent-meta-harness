# Backlog

Active planning now targets **AI Agent Meta-Harness v2**.

The previous backlog has been frozen under `../archive/v1/backlog/`. Those
records are historical trace evidence, not active work to carry forward item by
item.

Use `v2-roadmap.md` as the single active roadmap until the v2 transition grows
large enough to justify splitting by ownership area.

## Transition Rules

- Do not automatically migrate v1 backlog items.
- Extract requirements from v1 only when they explain a repeated failure mode or
  a v2 packet/checker requirement.
- Prefer one active v2 roadmap item at a time until the packet lifecycle exists.
- Keep v1 records immutable except for archive index or path-repair work.

This project operationalizes Meta-Harness paper principles into a practical
harness toolkit, runtime adapters, and verification gates; it is not a paper
reproduction package or a claim that this local repo has demonstrated the
paper's benchmark gains.
